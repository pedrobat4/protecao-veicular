[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — todas as rotas servem o index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
